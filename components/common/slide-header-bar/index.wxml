<view class='slide-h-container'>
    <van-tabs bind:click='tabClick'>
        <block wx:for="{{tabs}}" wx:key="id">
            <van-tab title="{{item.name}}"></van-tab>
        </block>
    </van-tabs>
</view>